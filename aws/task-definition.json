{
  "family": "wingstop-poc",
  "networkMode": "awsvpc",
  "requiresCompatibilities": [
    "FARGATE"
  ],
  "cpu": "1024",
  "memory": "2048",
  "executionRoleArn": "<+pipeline.variables.executionRoleArn>",
  "taskRoleArn": "<+pipeline.variables.taskRoleArn>",
  "containerDefinitions": [
    {
      "name": "wingstop-poc",
      "image": "<+execution.steps.push_ecr.artifact_push_ecr.stepArtifacts.publishedImageArtifacts[0].url>",
      "cpu": 1024,
      "memory": 2048,
      "essential": true,
      "portMappings": [
        {
          "containerPort": 8080,
          "hostPort": 8080,
          "protocol": "tcp"
        }
      ]
    }
  ]
}